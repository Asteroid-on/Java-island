# 灵动岛使用指南

## 🚀 快速开始

### 第一步：编译项目

打开命令行（CMD或PowerShell），进入项目目录，执行：

```bash
mvn clean package
```

编译成功后，会在 `target` 目录下生成 `Java-island-1.0-SNAPSHOT.jar` 文件。

### 第二步：启动灵动岛

双击运行 `start-daemon.vbs` 文件

- ✅ 完全后台运行，不显示任何窗口
- ✅ 关闭终端后继续运行
- ✅ 系统托盘图标控制

### 第三步：使用灵动岛

1. **找到托盘图标**：在屏幕右下角的系统托盘中找到 🏝 图标

2. **显示灵动岛**：将鼠标移动到屏幕顶部中央位置，灵动岛会自动显示

3. **隐藏灵动岛**：将鼠标移开，灵动岛会自动隐藏

4. **退出程序**：右键点击托盘图标，选择"退出"菜单项

---

## ⚙️ 功能说明

### 自动显示
- 鼠标移到屏幕顶部中央 → 自动显示灵动岛
- 检测范围：岛的宽度 + 左右各扩展10像素
- 触发距离：距离顶部50像素内

### 动画效果
- 展开动画：从小球向下移动，然后向两边展开
- 收起动画：从两边向中心收束成球，然后向上消失
- 帧率：120fps 极致流畅

### 系统托盘
- 左键点击：显示/隐藏灵动岛
- 右键菜单：显示/隐藏、退出程序

---

## 🔧 常见问题

### Q1: 双击启动脚本后没有反应？
**A:** 
- 确认已执行 `mvn clean package` 编译项目
- 检查 `target` 目录下是否有 `Java-island-1.0-SNAPSHOT.jar` 文件
- 查看任务管理器中是否有 `javaw.exe` 进程

### Q2: 看不到托盘图标？
**A:**
- 检查系统托盘是否折叠，点击小箭头展开
- 确认编译成功，JAR文件存在
- 尝试重新编译并启动

### Q3: 灵动岛不显示？
**A:**
- 确保鼠标移到屏幕顶部中央位置
- 检查鼠标是否在岛的宽度范围内（包括左右扩展的10像素）
- 确认程序正在运行（查看托盘图标）

### Q4: 如何调整灵敏度？
**A:**
编辑 `SystemTrayManager.java` 第12行：
```java
private static final int TRIGGER_DISTANCE = 50; // 改为其他值
```
数值越小，需要越靠近顶部才能触发。

### Q5: 可以开机自启动吗？
**A:**
可以！将 `start-daemon.vbs` 创建快捷方式，放到Windows启动文件夹：
1. 右键 `start-daemon.vbs` → 发送到 → 桌面快捷方式
2. 按 `Win + R`，输入 `shell:startup`，回车
3. 将快捷方式复制到打开的文件夹

### Q6: 如何停止灵动岛？
**A:**
- **推荐**：右键托盘图标 → 选择"退出"
- **备选**：任务管理器 → 找到 `javaw.exe` → 结束进程

---

## 📁 项目结构

```
Java-island/
├── src/main/java/              # 源代码
│   ├── Main.java               # 主窗口类
│   ├── SystemTrayManager.java  # 系统托盘和鼠标监听
│   └── DynamicIslandService.java # 业务逻辑服务
├── target/                     # 编译输出目录
├── start-daemon.vbs            # 启动脚本 ⭐
├── README.md                   # 项目说明文档
└── pom.xml                     # Maven配置
```

---

## 💡 技术说明

### javaw vs java
- **java**: 启动Java应用，显示控制台窗口
- **javaw**: 启动Java应用，不显示控制台窗口（Windows专用）

### VBScript原理
```vbscript
objShell.Run "javaw -jar ""app.jar""", 0, False
```
- 参数 `0`: 隐藏窗口
- 参数 `False`: 非阻塞执行（立即返回）

---

## 🎯 配置修改

### 修改岛的大小
编辑 `Main.java` 和 `DynamicIslandService.java`：
```java
setSize(180, 50); // 宽度180px，高度50px
```

### 修改动画速度
编辑 `SystemTrayManager.java` 中的 duration 值：
```java
final double durationPhase1 = 5.5;  // 数值越小速度越快
final double durationPhase2 = 18.67;
```

### 修改触发区域
编辑 `SystemTrayManager.java`：
```java
// 左右扩展像素数（当前为10）
mouseLocation.x >= islandLocation.x - 10
mouseLocation.x <= islandLocation.x + islandWidth + 10
```

---

## 📞 需要帮助？

查看详细文档：[README.md](./README.md)

祝使用愉快！🏝️
