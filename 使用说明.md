# 云隙泡使用指南

## 🚀 快速开始

### 第一步：编译项目

打开命令行（CMD或PowerShell），进入项目目录，执行：

```bash
mvn clean package
```

编译成功后，会在 `target` 目录下生成 `Java-island-1.2.3.jar` 文件。

### 第二步：启动云隙泡

- **正式版（打包 exe）**：双击运行 `Java-island.exe`（jpackage 产物，守护进程已随包分发）
- **开发版（源码运行，进程名同为 Java-island）**：

```bash
powershell -ExecutionPolicy Bypass -File launch.ps1            # 静默启动（stdout → target\app-out.log）
powershell -ExecutionPolicy Bypass -File launch.ps1 -Console   # 带控制台窗口（交互调试）
```

应用启动时自动拉起并管理六个守护进程（MediaInfoDaemon / WechatNotifyDaemon / QqNotifyDaemon / ncm-server / QQMusicapi / qishui-api，端口/进程探测防重复），托盘退出时自动回收。首次运行会默认勾选开机自启（可在 设置 → 常规 中关闭）。

- ✅ 完全后台运行，不显示任何窗口
- ✅ 系统托盘图标控制

### 第三步：使用云隙泡

1. **找到托盘图标**：在屏幕右下角的系统托盘中找到 🏝 图标

2. **显示云隙泡**：将鼠标移动到屏幕顶部中央位置，云隙泡会自动显示

3. **隐藏云隙泡**：将鼠标移开，云隙泡会自动隐藏

4. **退出程序**：右键点击托盘图标，选择"退出"菜单项

---

## ⚙️ 功能说明

### 自动显示
- 鼠标移到屏幕顶部中央 → 自动显示云隙泡
- 检测范围：岛的宽度 + 左右各扩展10像素
- 触发距离：距离顶部 8 像素内
- 全屏适配：前台为无边框全屏窗口（如游戏）时岛不弹出，进入全屏后已显示的岛自动收起，退出全屏后恢复正常

### 动画效果
- 展开动画：从小球向下移动，然后向两边展开
- 收起动画：从两边向中心收束成球，然后向上消失
- 帧率：设计 100FPS（实测 ≈87FPS，高精度定时器驱动）

### 系统托盘
- 左键点击：显示/隐藏云隙泡
- 右键菜单：设置、退出程序

### 自动更新
- 启动后后台自动检测 GitHub Releases 最新版本（延迟 30s，不阻塞启动），发现新版本托盘气泡提醒（同一版本仅提醒一次）
- 手动入口：托盘菜单 → 设置 → 「更新」页签：检查更新、查看发布说明、打开发布页
- 下载便携包：进度条实时显示 + 中途取消；下载完成后可「打开下载文件夹」或「解压并打开」（解压到 Downloads 目录）
- 便携包为 zip 压缩包：解压后替换旧版本文件夹即可完成更新

### 微信/QQ 消息提醒（灵动岛风格）
- 收到微信或 QQ PC 端新消息时，主岛自动弹出：左侧对应图标 + 「微信 / 你收到了一条微信消息」「QQ / 发送者：消息内容」（QQ 群消息含群名），5 秒后自动收起
- **点击岛** → 直接跳转打开微信 App
- 连续消息去抖：展示期间再来新消息不重启动画，仅刷新计时器

**实现原理（零注入、零 Hook、不封号）**：
由 .NET 8 守护进程 `WechatNotifyDaemon` / `QqNotifyDaemon` 调用 Windows 官方公开 API `UserNotificationListener`，被动读取 Windows 通知中心内的微信/QQ Toast 通知（Windows 10 1607+ 支持），不注入客户端进程、不 Hook、不读取内存，无任何封号风险。Java 侧经 WatchService 事件驱动 + 轮询兜底双通道接收，弹窗秒级抑制与连续消息去重防刷屏。

**使用前提**：
1. 微信/QQ PC 端需开启 Windows 系统通知（客户端设置 → 消息通知 → Windows 通知）
2. 首次运行若被 Windows 拦截，请到 设置 → 系统 → 通知 中允许云隙泡读取通知

---

## 🎵 音乐功能与歌词来源

云隙泡在检测到音乐播放时自动弹出音乐面板，歌词来自三个本地歌词服务项目 + 一个公共兜底源，由 Java 侧 `LyricsService` → `LyricsDispatcher` 按播放器来源（sourceAppId）自动调度：

```
LyricsDispatcher（按 sourceAppId 调度，本平台源失败 → LRCLIB 兜底）
├─ 网易云音乐/cloudmusic → ncm-server（本地代理，端口 3000）
├─ QQ音乐/qqmusic      → QQMusicapi（Node.js 项目，端口 3301）
├─ 汽水音乐             → qishui-api（Node.js 项目，端口 3300）
└─ 未知平台/全部失败    → LRCLIB（lrclib.net 公共歌词库，直接 HTTPS，不依赖本地服务）
```

### 项目一：ncm-server（网易云音乐 API 代理）

| 项 | 说明 |
|---|---|
| 产物 | `ncm-server.exe`（项目根目录，NeteaseCloudMusicApi 打包） |
| 原项目 | https://github.com/Binaryify/NeteaseCloudMusicApi |
| 端口 | 3000 |
| 运行时 | Node.js（已打包进 exe） |

Java 侧（`NeteaseLyricsProvider`）使用的接口：

| 接口 | 用途 |
|---|---|
| `GET /cloudsearch?keywords=...&type=1&limit=5` | 按 歌名+歌手 搜索，取 songId |
| `GET /lyric/new?id=...` | 取歌词（优先用 `lrc` 字段，标准 LRC 格式） |
| `GET /lyric?id=...` | 兜底（/lyric/new 无结果时） |

匹配规则：sourceAppId 含 `cloudmusic` / `netease` 时优先使用。

### 项目二：QQMusicapi（QQ音乐 API）

| 项 | 说明 |
|---|---|
| 位置 | `QQMusicapi/` 子目录（Node.js + Koa，JavaScript 版 QQMusicApi） |
| 原项目 | https://github.com/L-1124/QQMusicApi（Python 版；本目录为其 JavaScript 移植） |
| 端口 | 3301（环境变量 `PORT` 可改；3300 让位给 qishui-api） |
| 运行时 | 系统 Node.js ≥ 18（`node src/server.js`） |
| 依赖 | koa、@koa/router、koa-bodyparser、undici |
| 核心算法 | `zzc_sign` 签名、TripleDES 加解密、QRC 歌词解密 |
| 登录支持 | QQ 二维码登录 / 手机验证码登录（`credential.json`、`device.json` 持久化） |

Java 侧（`QQLyricsProvider`）使用的接口：

| 接口 | 用途 |
|---|---|
| `POST /search/byType` | 按 歌名+歌手 搜索，取 songMid |
| `GET /song/lyric?mid=...&decode=1` | 歌词（服务端自动 QRC 解密） |

歌词格式说明：QQ音乐歌词为 QRC 逐字格式（`[startMs,durationMs]文本(wordStart,wordDur)...`），Java 侧先做 QRC → LRC 转换，再统一解析。完整 Web API 列表见 `QQMusicapi/README.md`。

匹配规则：sourceAppId 含 `qqmusic` 时优先使用。

### 项目三：qishui-api（汽水音乐 API）

| 项 | 说明 |
|---|---|
| 位置 | `qishui-api/` 子目录（Node.js，入口 `app.js`，随包分发） |
| 原项目 | https://github.com/guowenye/qishui-api（汽水音乐逆向项目） |
| 端口 | 3300（`qishui-api/.env` 的 `PORT` 可改） |
| 运行时 | 系统 Node.js ≥ 18 |
| 登录 | 无需登录：签名搜索被上游拒绝时自动回退免签名 web 搜索，歌词走 H5 源 |

Java 侧（`QishuiLyricsProvider`）使用的接口：

| 接口 | 用途 |
|---|---|
| `GET /search/mixed?keywords=...` | 按 歌名+歌手 搜索取 track_id（PC 源失败自动回退免签名 web 搜索） |
| `GET /lyric?track_id=...` | 歌词（YRC 逐字格式，Java 侧自动转标准 LRC） |

歌词进度说明：汽水音乐的 SMTC 播放中不上报连续进度且状态高频抖动，应用内置本地位置估计器（同步锚点 + 挂钟外推）保证歌词逐句推进，拖动进度条按位置跳变自动校准；暂停时停在当前行，恢复后继续跟随。

匹配规则：sourceAppId 含 `sodamusic` / `qishui` / `luna.music` 或为中文「汽水音乐」时优先使用。

### 服务生命周期（守护进程管理）

- 应用启动时由 `IslandApplication.launchDaemons()` 依次检查并拉起：`MediaInfoDaemon.exe`（媒体信息）→ `WechatNotifyDaemon.exe` / `QqNotifyDaemon.exe`（微信/QQ 通知）→ `ncm-server.exe`（端口 3000）→ `QQMusicapi`（端口 3301）→ `qishui-api`（端口 3300）；对应端口已被占用或进程已在运行则跳过启动，日志输出重定向到 `daemon.log`。
- 应用退出（托盘“退出”）时 ShutdownHook 统一回收由本次应用拉起的六个守护进程。
- 歌词/封面结果缓存于 `cache/lyrics/`（内存 L1 + 磁盘 L2，按播放器隔离，LRU + TTL 管理）。
- 开机自启：首次运行默认开启（启动项缺失时自动修复注册），设置 → 常规 可随时关闭。

### ⚠️ 使用提示（网易云音乐）

1. **歌词上岛前置条件**：网易云音乐客户端需在设置中打开 SMTC 接口（系统媒体控制），否则 `MediaInfoDaemon` 检测不到播放状态，歌词无法上岛。
2. **歌词不跟随进度**：由于网易云音乐的 SMTC 接口不完整（不报告播放进度），歌词显示不能跟随进度条移动，只会展示整首歌词；这是网易云客户端 SMTC 实现的限制，非云隙泡问题。

### ⚠️ 使用提示（汽水音乐）

1. **无需登录或配置**：qishui-api 内置免签名 web 搜索回退与 H5 歌词源，播放即自动匹配歌词/封面。
2. **系统媒体服务挂起**：若歌词/封面完全无检测（汽水会话可能触发系统 GSMTC 服务挂起），重启系统即可恢复；应用已内置 2.5s 超时兜底不会卷死。

---

## 🔧 常见问题

### Q1: 启动后没有反应？
**A:**
- 确认已执行 `mvn clean package` 编译项目（开发版）
- 检查 `target` 目录下是否有 `Java-island-1.2.3.jar` 文件
- 查看任务管理器中有无 `Java-island.exe` 进程（开发版/正式版统一为此名）

### Q2: 看不到托盘图标？
**A:**
- 检查系统托盘是否折叠，点击小箭头展开
- 确认编译成功，JAR文件存在
- 尝试重新编译并启动

### Q3: 云隙泡不显示？
**A:**
- 确保鼠标移到屏幕顶部中央位置
- 检查鼠标是否在岛的宽度范围内（包括左右扩展的10像素）
- 确认程序正在运行（查看托盘图标）
- 游戏全屏时岛不显示是设计行为：前台为无边框全屏窗口（游戏）时岛暂停弹出，退出全屏后恢复

### Q4: 如何调整灵敏度？
**A:**
编辑 `config/AppConstants.java`：
```java
public static final int TRIGGER_DISTANCE = 8; // 距离屏幕顶部像素，越大越易触发
```

### Q5: 可以开机自启动吗？
**A:**
首次运行已默认开启（在启动文件夹创建带 `--autostart` 参数的快捷方式，启动项缺失/失效时会自动修复注册）。管理方式：
1. **推荐**：托盘菜单 → 设置 → 常规 → 勾选/取消「开机时自动启动」
2. **手动**：将 `Java-island.exe` 创建快捷方式放入启动文件夹：
   1. 右键 `Java-island.exe` → 发送到 → 桌面快捷方式
   2. 按 `Win + R`，输入 `shell:startup`，回车
   3. 将快捷方式复制到打开的文件夹

### Q6: 如何停止云隙泡？
**A:**
- **推荐**：右键托盘图标 → 选择"退出"（会同步回收六个守护进程）
- **备选**：任务管理器 → 找到 `Java-island.exe` → 结束进程

### Q7: 如何更新到新版本？
**A:**
- **自动提醒**：启动后检测到新版本会托盘气泡提醒，打开 设置 → 更新 下载便携包
- **手动检查**：托盘菜单 → 设置 → 「更新」页签 → 检查更新 → 下载 → 解压并打开
- 便携包下载到 Downloads 目录，解压后替换旧版本文件夹即可完成更新

---

## 📁 项目结构

```
Java-island/
├── src/main/java/com/island/   # 源代码
│   ├── IslandApplication.java  # 主入口（单实例锁/守护进程管理/退出回收/开机自启默认策略）
│   ├── island/ui/              # 主岛/扩展岛 UI、动画、音乐面板、微信弹窗
│   ├── music/                  # 歌词服务（调度/缓存/网易云/QQ音乐/汽水 Provider）
│   ├── battery/ bluetooth/     # 电池/蓝牙监控（JNA）
│   ├── wifi/ privacy/ weather/ # WiFi/隐私/天气监控
│   ├── wechat/ qq/             # 微信/QQ 消息通知（守护进程桥接/事件驱动+轮询兜底）
│   ├── tray/ monitor/ config/  # 托盘/监控调度/配置
│   └── update/                 # 自动更新（GitHub Releases 检测/下载/解压）
├── QQMusicapi/                 # QQ音乐 API 子项目（Node.js + Koa，端口 3301）
├── qishui-api/                 # 汽水音乐 API 子项目（Node.js，端口 3300，搜索/歌词/封面）
├── MediaInfoDaemon.exe         # SMTC 媒体信息守护进程（.NET 8）
├── WechatNotifyDaemon.exe      # 微信通知守护进程（.NET 8 UserNotificationListener）
├── QqNotifyDaemon.exe          # QQ 通知守护进程（.NET 8 UserNotificationListener）
├── ncm-server.exe              # 网易云 API 代理（端口 3000）
├── target/                     # 编译输出目录
├── launch.ps1                  # 开发版启动器（进程名 Java-island）⭐
├── README.md                   # 项目说明文档
└── pom.xml                     # Maven配置
```

---

## 💡 技术说明

### 进程名为什么是 Java-island
开发版通过 `launch.ps1` 将 JDK 的 `java.exe` 复制为 `target\Java-island.exe` 后启动（Windows 任务管理器中的进程名取自可执行文件名，JDK 25 启动器可在 JDK 目录外独立运行），因此开发版与 jpackage 打包版在任务管理器中统一显示为 **Java-island**。

### 静默启动原理
`launch.ps1` 默认用 `Start-Process -RedirectStandardOutput/Error` 把控制台输出重定向到 `target\app-out.log` / `app-err.log`，无控制台窗口且进程不随当前终端会话销毁；`-Console` 参数则打开可见控制台窗口用于交互调试。

---

## 🎯 配置修改

### 修改扩展岛大小与动画
编辑 `island/ui/IslandUiStyle.java`：
```java
public static final int EXPANDED_WIDTH = 450;  // 扩展岛宽度
public static final int EXPANDED_HEIGHT = 54;  // 扩展岛高度
public static final int EXPAND_ANIM_DURATION_MS = 280; // 展开/收起动画时长
public static final int EXPAND_ANIM_FRAME_MS = 10;     // 帧间隔（100FPS）
```

### 修改触发区域
编辑 `config/AppConstants.java`：
```java
// 距离屏幕顶部像素（当前为 8）
public static final int TRIGGER_DISTANCE = 8;
```
左右扩展像素在 `tray/SystemTrayManager.java` 的 MouseInfoMonitor 中（当前为 ±10）。

### 调试控制台输出
启动参数加 `-Disland.debug=true` 开启高频调试输出（默认静默）。

---

## 📞 需要帮助？

查看详细文档：[README.md](./README.md)

祝使用愉快！🏝️
